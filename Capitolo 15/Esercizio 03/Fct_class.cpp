#include "Fct_class.h"

/*
* Aggiungendo il template non sono riuscito a separare il template in due file e compilare.
* Il template deve essere dichiarato e definito nello stesso file.
*/

//using namespace Graph_lib;

//template<typename T>






/*void Fct_class::draw_lines() const {
	if (func != nullptr) func->draw();
}
void Fct_class::move(int dx, int dy) {
	if (func != nullptr) func->move(dx, dy);
}
*/